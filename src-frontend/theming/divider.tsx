import { COLOR_CONTENT_SECONDARY_SHADE_THREE } from "../styles/colors";

export const dividerTheme = {
  Divider: {
    baseStyle: {
      borderColor: COLOR_CONTENT_SECONDARY_SHADE_THREE,
      borderWidth: "1px",
    },
  },
};
