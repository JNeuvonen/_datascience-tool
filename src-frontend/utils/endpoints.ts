const PROJECT = "project";
const DATASET = "dataset";

export const BASE_URL = "http://localhost:8000";

export const URLS = {
  project_dataset: `${BASE_URL}/${PROJECT}/${DATASET}`,
  project: `${BASE_URL}/${PROJECT}`,
};
